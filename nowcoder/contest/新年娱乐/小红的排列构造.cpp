#include <bits/stdc++.h>
void solve()
{
    int n, k;   std::cin >> n >> k;
    for (int i = 1; i <= n; i++)
        std::cout << i << " ";
}

signed main()
{
    std::ios::sync_with_stdio(false);
    std::cin.tie(0);
    solve();
}